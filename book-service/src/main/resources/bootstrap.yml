spring:
  cloud:
    config:
      # 其实就是文件名
      name: bookservice
      #配置服务器的地址
      uri: http://127.0.0.1:8700
      #环境
      profile: dev
      #分支
      label: master
# http://127.0.0.1:8700/master/bookservice-dev.yml